spring:
  application:
    name: BuildersEurekaServer
  cloud:
    config:
      name: BuildersEurekaServer
      uri: http://localhost:8081
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
    register-with-eureka: false
    fetch-registry: false
server:
  port: 8761