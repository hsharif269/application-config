zuul:
  sensitive-headers: Cookie,Set-Cookie
  routes:
    userProfile:
      path: /userProfile/**
      url: http://localhost:8082

security:
  oauth2:
    resource:
      jwt:
        key-value: 123
